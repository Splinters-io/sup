[package]
name = "test-project"
version = "0.1.0"

[dependencies]
serde = "1.0.200"
tokio = { version = "1.37.0", features = ["full"] }

[dev-dependencies]
criterion = "0.5.1"

[build-dependencies]
cc = "1.0.90"
